# Issue 0007: Packaging - PATH symlink + README

> Labels: enhancement, ready-for-agent · Type: AFK

## Parent

[PRD: webtweak v1](../PRD.md)

## What to build

Make webtweak runnable from anywhere and document it. Symlink the `webtweak` executable onto PATH, and write the README covering usage (`webtweak <page.html>`), the capture-then-reconcile loop (how edits.json hands off to Claude), and v1's explicit limitations (single-viewport, no structural reordering, no copy editing).

## Acceptance criteria

- [ ] `webtweak` is executable and runnable by name from any directory
- [ ] README explains the `webtweak <page.html>` usage and the `--port` / `--no-browser` flags
- [ ] README explains the capture-then-reconcile loop and where the edits file lands
- [ ] README states v1 limitations (single-viewport base styles, no structural reordering, no copy editing)

## Blocked by

- Issue 0001 (walking skeleton)

---

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Make `webtweak` runnable from anywhere and document the tool and its loop.

**Current behavior:**
`webtweak` runs only by invoking its path directly under the project. There is no README, so the capture-then-reconcile loop and v1's limitations are undocumented outside the issue tracker.

**Desired behavior:**
`webtweak` is on PATH and runnable by name from any directory. A README explains usage (`webtweak <page.html>`, the `--port` and `--no-browser` flags), the capture-then-reconcile loop (how the `*.webtweak.json` edits file hands off to Claude), and v1's explicit limitations (single-viewport base styles, no structural reordering, no copy editing).

**Key interfaces:**
- A symlink placing the `webtweak` executable on PATH (a user-writable bin directory already on PATH is preferred over a system location).
- A project README at the repo root.

**Acceptance criteria:**
- [ ] `webtweak` is executable and runnable by name from any directory
- [ ] README documents `webtweak <page.html>` usage and the `--port` / `--no-browser` flags
- [ ] README explains the capture-then-reconcile loop and where the edits file lands
- [ ] README states v1 limitations (single-viewport base styles, no structural reordering, no copy editing)

**Out of scope:**
- Packaging as a pip/npm distributable or adding an installer
- CI / release automation
